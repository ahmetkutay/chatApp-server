name: OmniaPotentia CI/CD Pipeline

on:
  push:
    branches:
      - master
      - integration
      - development

jobs:
  build-master:
    name: Build and Deploy (Master)
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/master'

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        with:
          node-version: 16

      - name: Install dependencies
        run: npm ci

      - name: Build project
        run: npm run build

  build-integration:
    name: Build and Deploy (Integration)
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/integration'

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        with:
          node-version: 16

      - name: Install dependencies
        run: npm ci

      - name: Build project
        run: npm run build

  build-development:
    name: Build and Test (Development)
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/development'

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        with:
          node-version: 16

      - name: Install dependencies
        run: npm ci

      - name: Build project
        run: npm run build

      - name: Run tests
        run: npm test
